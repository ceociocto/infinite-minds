# AI Agent System Environment Variables
# Copy this file to .env.local and fill in your actual values

# ============================================================================
# ZHIPU AI CONFIGURATION (Required for AI Agent functionality)
# ============================================================================
# Get your API key from: https://open.bigmodel.cn/
# 1. Register an account at https://open.bigmodel.cn/
# 2. Navigate to API Keys section
# 3. Create a new API key
# 4. Paste it below (without quotes)

ZHIPU_API_KEY=your_zhipu_api_key_here

# ============================================================================
# GITHUB TOKEN (Optional - for GitHub workflow features)
# ============================================================================
# Get your personal access token from: https://github.com/settings/tokens
# 1. Go to GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
# 2. Generate new token (classic)
# 3. Select scopes: repo (for full repository access)
# 4. Paste it below (without quotes)

GITHUB_TOKEN=your_github_token_here

# ============================================================================
# ADDITIONAL NOTES
# ============================================================================
# - ZHIPU_API_KEY is required for AI agent functionality
# - Without it, the system will run in simulation mode
# - GITHUB_TOKEN is only needed for GitHub-related workflows
# - Never commit .env.local to version control (it's in .gitignore)
# - Supported models: glm-4-flash (fast/cheap), glm-4 (high quality), glm-4v (vision)
